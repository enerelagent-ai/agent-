DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/real_estate

# Optional -- unset (the default) leaves every route open, for local dev.
# Set both in production (Render) to require Basic Auth on every
# listings/dashboard route. See app/api/deps.py:require_admin.
# ADMIN_USERNAME=
# ADMIN_PASSWORD=
